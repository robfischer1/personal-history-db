[phdb]
manifest_version = 1

[plugin]
name = "apple_notes_full"
version = "0.1.0"
description = "Apple Notes full-body extraction — gunzip ZICNOTEDATA.ZDATA + proto 2->3->2"
kind = "source"
entry_point = "phdb.plugins.apple_notes_full:AppleNotesFullPlugin"

[source]
emits = ["DigitalDocument"]
entity_refs = []
formats_used = ["apple_notes_sqlite"]
records_required = ["DigitalDocument"]
sidecars = []
embeddable_tables = []
facets_projected = ["Time"]
