[phdb]
manifest_version = 1

[plugin]
name = "google_drive"
version = "0.4.0"
description = "Google Drive Takeout ZIP — per-file body extraction via document_extract"
kind = "source"
entry_point = "phdb.plugins.google_drive:GoogleDrivePlugin"

[source]
emits = ["DigitalDocument"]
entity_refs = []
formats_used = ["google_drive_zip"]
records_required = ["DigitalDocument"]
sidecars = []
embeddable_tables = []
facets_projected = ["Time"]
