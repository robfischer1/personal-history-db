[phdb]
manifest_version = 1

[plugin]
name = "readaction"
version = "0.4.0"
description = "Stub plugin for Pocket / Instapaper / future read-it-later reading-list sources; schema ready, parser pending"
kind = "source"
entry_point = "phdb.plugins.readaction:ReadActionPlugin"

[source]
emits = ["ReadAction"]
entity_refs = ["web_pages"]
formats_used = []
records_required = []
sidecars = []
embeddable_tables = []
facets_projected = ["Time"]
